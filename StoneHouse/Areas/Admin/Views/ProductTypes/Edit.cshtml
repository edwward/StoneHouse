@model StoneHouse.Models.ProductTypes
@{
    ViewData["Title"] = "Edit";
}

<br />
<h2 class="text-info">Edit Product Type</h2>

<!-- action metoda edit v producttypes controlleru -->
<form method="post" asp-action="Edit">
    <div class="p-4 border rounded">
        <!-- id je zde pro action edit metodu, ktera v parametru potrebuje id produktu -->
        <input type="hidden" asp-for="Id" />
        <div asp-validation-summary="ModelOnly" class="text-danger"></div>
        <div class="form-group row">
            <div class="col-2">
                <label asp-for="Name"></label>
            </div>
            <div class="col-5">
                <input asp-for="Name" class="form-control" />
            </div>
            <span asp-validation-for="Name" class="text-danger"></span>
        </div>
        <br />
        <div class="form-group">
            <input type="submit" class="btn btn-primary" asp-route-id="@Model.Id" value="Update" />
            <a asp-action="Index" class="btn btn-success">Back to List</a>
        </div>
    </div>
</form>

@section Scripts{
    @{ await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}

